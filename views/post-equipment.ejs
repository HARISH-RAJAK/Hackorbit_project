<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Post Equipment</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      background-image: linear-gradient(to bottom right, #ecfdf5, #d1fae5);
    }
  </style>
</head>

<body class="min-h-screen text-gray-800 font-sans">

  <!-- Header -->
  <header class="bg-green-500 text-white p-4 shadow-md flex justify-between items-center">
    <a href="/renting">
      <button onclick="history.back()"
        class="bg-black text-white px-3 py-1 rounded-md text-sm font-medium hover:bg-gray-800 transition">
        ← Back
      </button>
    </a>
    <h1 class="text-xl font-bold text-center flex-grow -ml-12">FarmConnect - Post Equipment</h1>
    <div class="w-[80px]"></div> <!-- Spacer to balance the back button -->
  </header>

  <!-- Form Card -->
  <div class="max-w-3xl mx-auto mt-10 bg-white p-8 rounded-2xl shadow-xl border border-green-300">

    <h2 class="text-3xl font-bold text-green-700 mb-6 text-center">Post Equipment for Rent</h2>

    <form id="postForm" action="/post-form" method="POST" enctype="multipart/form-data" class="grid grid-cols-1 sm:grid-cols-2 gap-6">


      <div>
        <label class="block text-sm font-semibold text-gray-700">Owner Name</label>
        <input type="text" name="owner" required
          class="mt-1 w-full border border-green-300 rounded-xl p-2 focus:outline-none focus:ring-2 focus:ring-green-400" />
      </div>


      <div>
        <label class="block text-sm font-semibold text-gray-700">Machine Name</label>
        <input type="text" name="machineName" required
          class="mt-1 w-full border border-green-300 rounded-xl p-2 focus:outline-none focus:ring-2 focus:ring-green-400" />
      </div>

      <!-- Machine Type -->
      <div>
        <label class="block text-sm font-semibold text-gray-700">Machine Type</label>
        <select name="machineType"
          class="mt-1 w-full border border-green-300 rounded-xl p-2 focus:outline-none focus:ring-2 focus:ring-green-400">
          <option>Tractor</option>
          <option>Harvester</option>
          <option>Plough</option>
          <option>Tiller</option>
          <option>Other</option>
        </select>
      </div>

      <!-- Rent -->
      <div>
        <label class="block text-sm font-semibold text-gray-700">Rent per Day (₹)</label>
        <input type="number" name="rent" required
          class="mt-1 w-full border border-green-300 rounded-xl p-2 focus:outline-none focus:ring-2 focus:ring-green-400" />
      </div>

      <!-- Location -->
      <div>
        <label class="block text-sm font-semibold text-gray-700">Location</label>
        <input type="text" name="location" required
          class="mt-1 w-full border border-green-300 rounded-xl p-2 focus:outline-none focus:ring-2 focus:ring-green-400" />
      </div>

      <!-- Availability -->
      <div>
        <label class="block text-sm font-semibold text-gray-700">Availability</label>
        <select name="status"
          class="mt-1 w-full border border-green-300 rounded-xl p-2 focus:outline-none focus:ring-2 focus:ring-green-400">
          <option value="available">Available</option>
          <option value="booked">Booked</option>
        </select>
      </div>

      
      <div>
        <label class="block text-sm font-semibold text-gray-700">Rating (0 - 5)</label>
        <input type="number" name="rating" step="0.1" min="0" max="5"
          class="mt-1 w-full border border-green-300 rounded-xl p-2 focus:outline-none focus:ring-2 focus:ring-green-400" />
      </div>

      
      <div class="sm:col-span-2">
        <label class="block text-sm font-semibold text-gray-700">Upload Image</label>
        <input type="file" name="image" accept="image/*"
          class="mt-1 w-full border border-green-300 rounded-xl p-2 focus:outline-none focus:ring-2 focus:ring-green-400" />
      </div>

      <div class="sm:col-span-2">
        <button type="submit"
          class="w-full bg-green-500 hover:bg-amber-600 text-white font-semibold py-2 px-4 rounded-xl transition duration-200 shadow-md">
          Post Equipment
        </button>
      </div>
    </form>
  </div>

</body>

</html>